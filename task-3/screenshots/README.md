# Required Job screenshots

Save the real images with exactly these names:

1. `job_task_configuration.png` — Git URL, branch, `task-2` project directory,
   dbt commands, warehouse, catalog, and schema.
2. `job_successful_run.png` — green run and dbt output.
3. `job_paused_schedule.png` — schedule visible with status Paused.
